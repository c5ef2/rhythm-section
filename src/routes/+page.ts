// Rendered entirely client-side so the first paint already reflects the
// user's stored settings (localStorage is only available in the browser).
export const ssr = false;
export const prerender = false;
